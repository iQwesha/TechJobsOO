@model TechJobs.ViewModels.JobFieldsViewModel

@{
// TODO #2.1- declare the ViewModel (outside of this @{ } block)
}

<table class="job-listing">
    @{ 
// TODO #2.2 - display the job fields, one per row (outside of this @{ } block)
    }

<form asp-controller="Job" asp-action="New" method="post">
    <div class="form-group">
        <label asp-for="Name"></label>
        <input class="form-control" asp-for="Name" />
    </div>
    <div class="form-group">
        <label asp-for="EmployerID"></label>
        <select asp-for="EmployerID" asp-items="Model.Employers"></select>
    </div>
    <div class="form-group">
        <label asp-for="PositionType"></label>
        <select asp-for="PositionType" asp-items="Model.Employers"></select>
    </div>
    <div class="form-group">
        <label asp-for="CoreCompetency"></label>
        <select asp-for="CoreCompetency" asp-items="Model.Employers"></select>
    </div>
    <div class="form-group">
        <label asp-for="All"></label>
        <select asp-for="All" asp-items="Model.Employers"></select>
    </div>

    <input type="submit" value="Add Job" />
</form>
</table>
